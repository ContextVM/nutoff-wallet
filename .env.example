# Coco Cashu Wallet MCP Server Configuration
# Copy this file to .env and update the values (all are optional)

# OPTIONAL: BIP-39 mnemonic seed phrase for wallet
# If not provided, a secure seed will be automatically generated and saved to .env
COCO_SEED="your secure bip39 mnemonic here"

# OPTIONAL: Database file path (default: "./coco.db")
COCO_DATABASE_PATH="./coco.db"

# OPTIONAL: MCP Server configuration
MCP_SERVER_NAME="cashu-wallet-mcp-server"
MCP_SERVER_VERSION="1.0.0"

# OPTIONAL: Logging level (debug, info, warn, error)
LOG_LEVEL="info"

# Nostr Server Transport Configuration
# REQUIRED for Nostr transport: Server private key (hex format) If not provided the server will generate a new key each time it starts
SERVER_PRIVATE_KEY=""

# OPTIONAL: Nostr relays (comma-separated, default: "wss://relay.contextvm.org")
SERVER_RELAYS="wss://relay.contextvm.org"

# OPTIONAL: Allowed public keys (comma-separated hex keys)
# If empty, all public keys are allowed. For security reasons, it is recommended to set this to a specific public key or set of public keys
ALLOWED_PUBLIC_KEYS=""